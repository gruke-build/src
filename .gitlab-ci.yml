image: mcr.microsoft.com/dotnet/sdk:10.0.103

variables:
  GIT_DEPTH: 0
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - build

build:
  stage: build
  script:
    - './build.cmd Test'
  artifacts:
    paths:
      - output/
      - output/test-results/
