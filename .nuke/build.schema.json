{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "definitions": {
    "Host": {
      "type": "string",
      "enum": [
        "AppVeyor",
        "AzurePipelines",
        "Bamboo",
        "Bitbucket",
        "Bitrise",
        "ForgejoActions",
        "GitHubActions",
        "GitLab",
        "Jenkins",
        "Rider",
        "SpaceAutomation",
        "TeamCity",
        "Terminal",
        "TravisCI",
        "VisualStudio",
        "VSCode",
        "WoodpeckerCI"
      ]
    },
    "ExecutableTarget": {
      "type": "string",
      "enum": [
        "Announce",
        "AnnounceDiscord",
        "Changelog",
        "Clean",
        "Compile",
        "CreateForgejoRelease",
        "CreateGitHubRelease",
        "CreateNotification",
        "DeleteGitLabCIPackages",
        "DeletePackages",
        "DocFx",
        "DownloadLicenses",
        "GeneratePublicApi",
        "GenerateTools",
        "Hotfix",
        "Install",
        "InstallFonts",
        "Kiota",
        "Milestone",
        "Pack",
        "Publish",
        "References",
        "Release",
        "ReleaseImage",
        "ReportCoverage",
        "ReportDuplicates",
        "ReportIssues",
        "Restore",
        "RunTargetInDockerImageTest",
        "Sandbox",
        "Test",
        "UpdateContributors",
        "UpdateStargazers"
      ]
    },
    "Verbosity": {
      "type": "string",
      "description": "",
      "enum": [
        "Verbose",
        "Normal",
        "Minimal",
        "Quiet"
      ]
    },
    "NukeBuild": {
      "properties": {
        "Continue": {
          "type": "boolean",
          "description": "Indicates to continue a previously failed build attempt"
        },
        "Help": {
          "type": "boolean",
          "description": "Shows the help text for this build assembly"
        },
        "Host": {
          "description": "Host for execution. Default is 'automatic'",
          "$ref": "#/definitions/Host"
        },
        "NoLogo": {
          "type": "boolean",
          "description": "Disables displaying the NUKE logo"
        },
        "Partition": {
          "type": "string",
          "description": "Partition to use on CI"
        },
        "Plan": {
          "type": "boolean",
          "description": "Shows the execution plan (HTML)"
        },
        "Profile": {
          "type": "array",
          "description": "Defines the profiles to load",
          "items": {
            "type": "string"
          }
        },
        "Root": {
          "type": "string",
          "description": "Root directory during build execution"
        },
        "Skip": {
          "type": "array",
          "description": "List of targets to be skipped. Empty list skips all dependencies",
          "items": {
            "$ref": "#/definitions/ExecutableTarget"
          }
        },
        "Target": {
          "type": "array",
          "description": "List of targets to be invoked. Default is '{default_target}'",
          "items": {
            "$ref": "#/definitions/ExecutableTarget"
          }
        },
        "Verbosity": {
          "description": "Logging verbosity during build execution. Default is 'Normal'",
          "$ref": "#/definitions/Verbosity"
        }
      }
    }
  },
  "allOf": [
    {
      "properties": {
        "AnnouncementPing": {
          "type": "string",
          "description": "a role ID or @everyone"
        },
        "AutoStash": {
          "type": "boolean",
          "description": "Automatically stash changes when using branch-related targets"
        },
        "CleanOutput": {
          "type": "boolean",
          "description": "Clean Kiota output before running the generator"
        },
        "CodecovToken": {
          "type": "string",
          "description": "codecov.io API token",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "Configuration": {
          "type": "string",
          "description": "The release configuration to use",
          "enum": [
            "Debug",
            "Release"
          ]
        },
        "DiscordWebhook": {
          "type": "string",
          "description": "the Discord webhook that should receive release notification embeds",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "FeedzNuGetApiKey": {
          "type": "string",
          "description": "feedz.io API key",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "ForgejoReleaseForgejoHostName": {
          "type": "string",
          "description": "Host of the Forgejo server. Only required if the current repo is not cloned from Forgejo, or you want to use a different server for releases"
        },
        "ForgejoReleaseForgejoOwner": {
          "type": "string",
          "description": "Owner of the Forgejo repository. Only required if the current repo is not cloned from Forgejo, or you want to use a different repository for releases"
        },
        "ForgejoReleaseForgejoRepoName": {
          "type": "string",
          "description": "Name of the Forgejo repository. Only required if the current repo is not cloned from Forgejo, or you want to use a different repository for releases"
        },
        "ForgejoReleaseForgejoToken": {
          "type": "string",
          "description": "Forgejo API token with appropriate API access",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "GitHubReleaseGitHubOwner": {
          "type": "string",
          "description": "Owner of the GitHub repository. Only required if the current repo is not cloned from GitHub, or you want to use a different repository for releases"
        },
        "GitHubReleaseGitHubRepoName": {
          "type": "string",
          "description": "Name of the GitHub repository. Only required if the current repo is not cloned from GitHub, or you want to use a different repository for releases"
        },
        "GitHubReleaseGitHubToken": {
          "type": "string",
          "description": "GitHub API token with appropriate API access",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "IgnoreFailedSources": {
          "type": "boolean",
          "description": "Ignore unreachable sources during Restore"
        },
        "Major": {
          "type": "boolean",
          "description": "Is this a major (x.0.0) release?"
        },
        "PublicNuGetApiKey": {
          "type": "string",
          "description": "nuget.org API key",
          "default": "Secrets must be entered via 'gruke :secrets [profile]'"
        },
        "Solution": {
          "type": "string",
          "description": "Path to a solution file that is automatically loaded"
        },
        "TestDegreeOfParallelism": {
          "type": "integer",
          "description": "Degree of parallelism for test execution",
          "format": "int32"
        }
      }
    },
    {
      "$ref": "#/definitions/NukeBuild"
    }
  ]
}
